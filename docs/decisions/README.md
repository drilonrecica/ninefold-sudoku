# Architecture decision records

`docs/ARCHITECTURE.md` records the initial architecture baseline.

Create an ADR here only when a later decision:

- changes an expensive-to-reverse baseline;
- introduces a new architectural constraint;
- resolves a material trade-off not already covered by the canonical specifications.

Do not duplicate the initial architecture into boilerplate ADRs.
